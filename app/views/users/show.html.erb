<div class="container">
	<p id="notice">Welcome! You have signed up successfully.</p>
	<%= render 'users/user_new', user: @user, book: @book %>
	<%= render 'books/list', user: @user, book: @book, books: @books %>
</div>
